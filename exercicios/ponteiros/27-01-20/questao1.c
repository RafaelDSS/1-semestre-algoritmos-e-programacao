#include <stdio.h>


int soma(int a, int b);

int main() {
    int a=5, 
        b=8;

    printf("A soma de A e B e: %d\n", soma(a, b));

    return

}

int soma(int a, int b) {
    int soma;

    soma = a + b;

    return soma;
}
